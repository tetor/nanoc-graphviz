require 'spec_helper'

describe Nanoc::Graphviz do
  it 'has a version number' do
    expect(Nanoc::Graphviz::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
